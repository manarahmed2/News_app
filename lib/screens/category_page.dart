import 'package:flutter/material.dart';

class CategoryView extends StatelessWidget {
  const CategoryView({super.key, required String category});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
